1) select a project with no test cases
2) press the run button so that the launcher selection dialog shows up
3) select JUnit and press next

1) select a project with no test cases
2) press the run button so that the launcher selection dialog shows up
3) select JUnit and press next
fixed in the 1.0.2
