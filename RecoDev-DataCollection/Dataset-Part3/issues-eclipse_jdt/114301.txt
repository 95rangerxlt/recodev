when multiple selecting test cases on the outline, run as junit only runs the
first method. while it should run all selected methods (preferrably) or be
disabled because of multiple selection. the current behaviour is misleading

when multiple selecting test cases on the outline, run as junit only runs the
first method. while it should run all selected methods (preferrably) or be
disabled because of multiple selection. the current behaviour is misleading
Running on multiple selection wasn't intended. In 3.2 the enablement of the 'Run As JUnit test' has been corrected and the menu doesn't show up anymore.
