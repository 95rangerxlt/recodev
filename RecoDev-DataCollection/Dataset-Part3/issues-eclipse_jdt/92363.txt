&R is used for both "Run a single test" and "Run"

I am quite used to hitting Alt-R to launch but this has no effect on this tab

&R is used for both "Run a single test" and "Run"

I am quite used to hitting Alt-R to launch but this has no effect on this tab
Fixed.
