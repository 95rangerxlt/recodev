HEAD: the package declaration is located after the import declarations

Steps to reproduce:

- Create class HashedMap which extends Map (don't use the default package)
- Set options to implement methods and constructors

HEAD: the package declaration is located after the import declarations

Steps to reproduce:

- Create class HashedMap which extends Map (don't use the default package)
- Set options to implement methods and constructors
fixed > 20050107
