Same mneumonic exists (alt-e) for the following checkbox choices:
"Insert spaces for tab (...)"
"Escape text when pasting into a string literal"

This probably wasn't caught b/c the "escape text..." one only applies when "wrap
java strings" is checked.

Same mneumonic exists (alt-e) for the following checkbox choices:
"Insert spaces for tab (...)"
"Escape text when pasting into a string literal"

This probably wasn't caught b/c the "escape text..." one only applies when "wrap
java strings" is checked.
available in builds > 20040415
