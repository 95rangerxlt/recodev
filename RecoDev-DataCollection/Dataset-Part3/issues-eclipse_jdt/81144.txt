I200412142000

In the following example, there is no quick fix for importing 'Target'

@Target
public @interface IModelling {
	
	boolean UML() default false;
	boolean platformDependent() default true;
	boolean OSDependent() default true;
}

I200412142000

In the following example, there is no quick fix for importing 'Target'

@Target
public @interface IModelling {
	
	boolean UML() default false;
	boolean platformDependent() default true;
	boolean OSDependent() default true;
}
Related to annotations not treated by organize import
new quick fix added > 20050131
*** Bug 84427 has been marked as a duplicate of this bug. ***
