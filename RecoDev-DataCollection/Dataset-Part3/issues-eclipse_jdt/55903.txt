200403240010

The import/export icons on the context menu are not the same as those on the
file menu.

200403240010

The import/export icons on the context menu are not the same as those on the
file menu.
Created attachment 8830
Ta da
This context menu is in Package Explorer, and JDT-UI hasn't received their icon
drop yet. I expect they will get duplicate icons for these. Moving to JDT-UI.
That's correct. JDT/UI will receive its icon set mid April.
JDT/UI received icons with new import/export icons.
