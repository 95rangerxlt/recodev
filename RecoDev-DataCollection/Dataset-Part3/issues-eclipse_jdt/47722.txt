When accidentially typing ';', Smart Semicolon places the caret at the next
logical position and inserts the semicolon. Backspace, however, only deletes the
semicolon but does not set the caret back to it's original position.

The same is true for Undo after Smart Semicolon.

When accidentially typing ';', Smart Semicolon places the caret at the next
logical position and inserts the semicolon. Backspace, however, only deletes the
semicolon but does not set the caret back to it's original position.

The same is true for Undo after Smart Semicolon.
adjusting milestone
Changing milestone to M9.
.
fixed > 20040423
