I am navigating through the word IContentProvider having "Smart cursor 
positioning in Java names" enables. However it skips over the word Content 
since it thinks that it's the same word which starts with the I.

Other examples include: URLClassLoader (here the word Class will be missed 
too).

Build id: 200405290105

I am navigating through the word IContentProvider having "Smart cursor 
positioning in Java names" enables. However it skips over the word Content 
since it thinks that it's the same word which starts with the I.

Other examples include: URLClassLoader (here the word Class will be missed 
too).

Build id: 200405290105
Seems to be fixed in 200406180010.
has been fixed for 3.0
