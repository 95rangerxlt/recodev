I20090310-0100.

JUnitCore.findTestTypes(IJavaElement, IProgressMonitor) does not pass on the progress monitor.

I20090310-0100.

JUnitCore.findTestTypes(IJavaElement, IProgressMonitor) does not pass on the progress monitor.
Fixed in HEAD.
Available in builds >= I20090311-0100.
