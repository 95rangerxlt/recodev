I20060301-0800

Eclipse calls these {} "braces". See preferences for code formatter and Java > Editor > Typing. We should not introduce a new term for the same thing.

Furthermore, quick assists that have a reverse action should have similar names. Since there's already "Change body statement to block", I'd suggest to call the reverse "Remove block from body statement".

I20060301-0800

Eclipse calls these {} "braces". See preferences for code formatter and Java > Editor > Typing. We should not introduce a new term for the same thing.

Furthermore, quick assists that have a reverse action should have similar names. Since there's already "Change body statement to block", I'd suggest to call the reverse "Remove block from body statement".
fixed > I20060301-0800

