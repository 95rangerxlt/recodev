This is likely a dup, but filed as a reminder.

The replace from local history action should provide a replace with previous 
action.

This is likely a dup, but filed as a reminder.

The replace from local history action should provide a replace with previous 
action.
I'm going to add a 'Compare with previous' too.
Then we no longer have the problem with the single entry submenu.
feature added for 20011220
fixed for milestone m1 (probably not in build 20011220).
*** Bug 5121 has been marked as a duplicate of this bug. ***
