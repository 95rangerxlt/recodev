1) select a static and a non-static method in the outline
2) Refactor>Move
->the wizard tells to move 2 static members.

1) select a static and a non-static method in the outline
2) Refactor>Move
->the wizard tells to move 2 static members.
checkActivation is called too early
need to understand what's going on
would like to find a fix
have a fix - will be in M6
fixed and released
