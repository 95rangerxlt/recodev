Use Extract Class on junit.runner.TestCaseClassLoader and generate alle Getters and Setters for all fields.
The initialization of field private String[] defaultExclusions= {
		"junit.framework.", 
		"junit.extensions.", 
		"junit.runner."
	};
will fail because the array creation needs to be inserted

Use Extract Class on junit.runner.TestCaseClassLoader and generate alle Getters and Setters for all fields.
The initialization of field private String[] defaultExclusions= {
		"junit.framework.", 
		"junit.extensions.", 
		"junit.runner."
	};
will fail because the array creation needs to be inserted
*** Bug 198420 has been marked as a duplicate of this bug. ***
It is not a dup.
Created attachment 75059
Patch
patch released > 20070731
