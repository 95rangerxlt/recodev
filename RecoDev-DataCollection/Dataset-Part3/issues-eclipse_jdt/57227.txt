When Eclipse is first installed and a new object is created, the automattically 
generated comments for date and author say something like:

"The change the generated code go to Window -> Preferences -> Java -> Code 
Comments ..."

This should now read:

"... Window -> Preferences -> Java -> Code Style..."

When Eclipse is first installed and a new object is created, the automattically 
generated comments for date and author say something like:

"The change the generated code go to Window -> Preferences -> Java -> Code 
Comments ..."

This should now read:

"... Window -> Preferences -> Java -> Code Style..."
got already fixed.
