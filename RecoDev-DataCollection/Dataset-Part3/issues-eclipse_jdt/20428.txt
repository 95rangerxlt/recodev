When switching on "Show all inherited members" in the list pane of the hierarchy
view, it would be very nice to have the members grouped by types, at least
optionally. 

That would be similar to the "Override methods" dialog, where grouping by types
is the default option.

When switching on "Show all inherited members" in the list pane of the hierarchy
view, it would be very nice to have the members grouped by types, at least
optionally. 

That would be similar to the "Override methods" dialog, where grouping by types
is the default option.
Got addressed for M5.
