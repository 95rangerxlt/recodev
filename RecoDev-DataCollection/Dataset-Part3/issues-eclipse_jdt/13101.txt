Create a *.java file in the default package with some non-externalized strings.
Run the tool.

Notice that it does not allow you to select the default package
as the target even if a *.properties file will not actually
be created because of your selections on the previous page.

Create a *.java file in the default package with some non-externalized strings.
Run the tool.

Notice that it does not allow you to select the default package
as the target even if a *.properties file will not actually
be created because of your selections on the previous page.
sorry, no time before M6
reopen
will not happen for 2.1
reopen to move to inbox
closing as 'P4, later' 
no action planned without an external contribution

Use of a default package is "deprecated" anyway.
Reopening...
Got addressed for 3.0.
