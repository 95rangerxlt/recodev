1. open the workspace preferences
2. select "Java"
3. select "Javadoc"
==> Bug 1: guideline is to never start a page with an error
    Bug 2 (severe): I can only leave the page with "Cancel" and have to reopen
the preference dialog.

1. open the workspace preferences
2. select "Java"
3. select "Javadoc"
==> Bug 1: guideline is to never start a page with an error
    Bug 2 (severe): I can only leave the page with "Cancel" and have to reopen
the preference dialog.
fixed for final build input 0319
