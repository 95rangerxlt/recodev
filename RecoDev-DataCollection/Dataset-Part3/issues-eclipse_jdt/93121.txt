I20050426

we should offer parameter hints for the type parameters to a generic type:

Map<K,V> map;

I20050426

we should offer parameter hints for the type parameters to a generic type:

Map<K,V> map;
we do this already for newly inserted type proposals, but not for Ctrl+Shift+Space
fixed > 20060703
