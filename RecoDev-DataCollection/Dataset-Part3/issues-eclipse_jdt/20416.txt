when the compilation unit name and type name do not match,
auto-fix proposes to rename the type name.
however it should also ername the constructor.

when the compilation unit name and type name do not match,
auto-fix proposes to rename the type name.
however it should also ername the constructor.
agreed, this was the old smart rename functionality. However, not critical 
enough for 2.0 - defer
*** Bug 22622 has been marked as a duplicate of this bug. ***
implemented > 20021008
