In M3

If I type "raw" into the project properties filter box, I expect to see the Java Compiler / Errors / Warnings page highlighted, so that I can edit the warning status for "Usage of a Raw Type".  It currently does not appear.

In M3

If I type "raw" into the project properties filter box, I expect to see the Java Compiler / Errors / Warnings page highlighted, so that I can edit the warning status for "Usage of a Raw Type".  It currently does not appear.
added 'raw' as keyword for the error/warning page > 20061110
