I don't see a possibility for entering a simple directory in the classpath for a
project, only jar files and so, but a directory would be useful - for finding
resources for example.

I don't see a possibility for entering a simple directory in the classpath for a
project, only jar files and so, but a directory would be useful - for finding
resources for example.
Class folders now do the trick.
