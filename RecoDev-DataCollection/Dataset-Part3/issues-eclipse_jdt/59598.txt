The wizards always call IRunnableContext with fork=false, we need to 
investigate where we can change fork=true.

The wizards always call IRunnableContext with fork=false, we need to 
investigate where we can change fork=true.
fixed for 20040426
