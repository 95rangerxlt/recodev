Created attachment 88766
first patch

3.4 M5

SimpleStyledCellLabelProvider is now offered by platform UI as API.
JDT (and search) can replace their own story.

Created attachment 88766
first patch

3.4 M5

SimpleStyledCellLabelProvider is now offered by platform UI as API.
JDT (and search) can replace their own story.
patch released > 20080208

open issues:
 - rendering matches in selected elements (mark occurrence)
 - colorizing the open type dialog.
I will file separate bugs for these
