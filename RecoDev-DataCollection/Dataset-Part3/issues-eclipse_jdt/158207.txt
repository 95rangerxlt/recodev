3.2
when the cleanup wizard is running, the numbers (xxx of yyy) are appended after the class name which makes them unreadable. 
If they were prepended (i.e., "(xxx of yyy) ClassName"), it would make them more useful.

3.2
when the cleanup wizard is running, the numbers (xxx of yyy) are appended after the class name which makes them unreadable. 
If they were prepended (i.e., "(xxx of yyy) ClassName"), it would make them more useful.
Fixed > I20061003-0800
