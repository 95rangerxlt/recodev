I20030717

- extract method is allowed in the following case:
  foo([f(), f()])

Must be disabled in this case.

I20030717

- extract method is allowed in the following case:
  foo([f(), f()])

Must be disabled in this case.
Fixed.
