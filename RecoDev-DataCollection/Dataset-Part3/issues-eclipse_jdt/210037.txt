Eclipse 3.4 M3 added the capability to detect unnecessary @SuppressWarnings annotations.

It should have a quick fix to remove the annotation.

Eclipse 3.4 M3 added the capability to detect unnecessary @SuppressWarnings annotations.

It should have a quick fix to remove the annotation.
added > 20071210
