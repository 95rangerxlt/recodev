Since we overwrite the options for classpath and sourcepath, we should warn if
the user attempts to enter values for those options in the dialog.

Since we overwrite the options for classpath and sourcepath, we should warn if
the user attempts to enter values for those options in the dialog.
Fixed.  Warning appears in status line.
