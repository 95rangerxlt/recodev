Build I20030604

The arrows in the JUnit view to step are only enabled if there are failures. If
the view only has errors then they are disabled.

Build I20030604

The arrows in the JUnit view to step are only enabled if there are failures. If
the view only has errors then they are disabled.
error count wasn't considered in enable check
