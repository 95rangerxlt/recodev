I200411022000

Setup a Java project with 5.0 compiler compliance but a 1.4 JRE container. the
New Enum wizard allows you to create an enum which results in compile errors.

I200411022000

Setup a Java project with 5.0 compiler compliance but a 1.4 JRE container. the
New Enum wizard allows you to create an enum which results in compile errors.
The wizard could giva a warning (while in the dialog) when java.lang.Enum is not
available
Created attachment 15664
Patch
released patch > 20041108
