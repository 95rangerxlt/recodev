Bring up the JavaDoc view on java.util.Iterator.  Notice that the HTML 
rendering gives up at 
<a href="{@docRoot}/../guide/collections/index.html">

Bring up the JavaDoc view on java.util.Iterator.  Notice that the HTML 
rendering gives up at 
<a href="{@docRoot}/../guide/collections/index.html">
Same for Javadoc hovers.

This will go away once we use the HTML widget.

does not work even if using Browser widget. Adapting summary.
*** Bug 64469 has been marked as a duplicate of this bug. ***
Fixed in HEAD.
