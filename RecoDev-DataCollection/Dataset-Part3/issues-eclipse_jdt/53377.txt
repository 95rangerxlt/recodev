The libraries dialog shown by the class path dialog needs to support activity 
based filtering, i.e., when PDE isn't active then the Required Plug-ins 
Container should not be shown.

The libraries dialog shown by the class path dialog needs to support activity 
based filtering, i.e., when PDE isn't active then the Required Plug-ins 
Container should not be shown.
Andre said he'll have a look at all jdt extension points
Created attachment 9560
Filter libraries based on activity

Not sure how far you got, but here is one piece.
fixed > 20071023
