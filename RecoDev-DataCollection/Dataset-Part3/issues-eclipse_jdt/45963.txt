This refactoring should allow changing the type of a "declaration element" 
(e.g. a local, field, arguments, return type, ...) to a sub or super type if 
possible.

This refactoring should allow changing the type of a "declaration element" 
(e.g. a local, field, arguments, return type, ...) to a sub or super type if 
possible.
Created attachment 6750
jdt.ui patch for 3.0 HEAD 11/11/2003 to add "Change Type"
Created attachment 6751
jdt.ui.tests.refactoring patch for 3.0 HEAD 11/11/2003 for "Change Type" JUnit tests
Released patches to HEAD
