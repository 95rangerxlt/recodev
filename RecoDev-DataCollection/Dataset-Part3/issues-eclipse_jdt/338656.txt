Created attachment 190134
Fix

HEAD

The "Change type of '*' to *" Quick Fix should offer matching type with type arguments first in the linked mode proposals.

Created attachment 190134
Fix

HEAD

The "Change type of '*' to *" Quick Fix should offer matching type with type arguments first in the linked mode proposals.
Fixed in HEAD.
