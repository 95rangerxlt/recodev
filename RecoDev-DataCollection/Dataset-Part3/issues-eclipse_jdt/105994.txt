Core shouldn't be depending on UI.  Do we even need this dependency??

Core shouldn't be depending on UI.  Do we even need this dependency??
apt builds and passes TestAll with the dependency removed, no other changes.  
I'm going to check it in and call it fixed.
