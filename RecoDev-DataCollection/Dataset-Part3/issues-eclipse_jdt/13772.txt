Build 20020412

In Java editor, move mouse pointer over Javadoc comment, you'll see lots of 
invocations of the JCore (hover-help) even though the pointer isn't moving.

You can see the background search activity involved in there by enabling the 
JCore indexer trace.

Build 20020412

In Java editor, move mouse pointer over Javadoc comment, you'll see lots of 
invocations of the JCore (hover-help) even though the pointer isn't moving.

You can see the background search activity involved in there by enabling the 
JCore indexer trace.
Build F2.
