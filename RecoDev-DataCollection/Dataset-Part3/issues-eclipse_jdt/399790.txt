BETA_JAVA8:

Now that the DOM/AST work (excluding ASTRewrite) is near completion for
JSR308, we can prepare for work on JSR335.

The present bug will be used as an umbrella bug for tracking the project.
Do not post patches here. Discussions are OK.

BETA_JAVA8:

Now that the DOM/AST work (excluding ASTRewrite) is near completion for
JSR308, we can prepare for work on JSR335.

The present bug will be used as an umbrella bug for tracking the project.
Do not post patches here. Discussions are OK.
*** Bug 384739 has been marked as a duplicate of this bug. ***
Resolving, any future bugs/issues in the DOM/AST APIs area for 335
can be raised as a follow up that blocks bug 380188
