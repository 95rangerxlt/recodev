M4

- create two projects X and XX
- switch to workingset mode
- put both into a working set
- select both and delete them

observe: one project is still rendered as a closed project.

M4

- create two projects X and XX
- switch to workingset mode
- put both into a working set
- select both and delete them

observe: one project is still rendered as a closed project.
Fixed
