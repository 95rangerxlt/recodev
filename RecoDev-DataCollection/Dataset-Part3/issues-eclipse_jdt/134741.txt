The line:

    if (("" + rootAttributes.get("type")).equalsIgnoreCase("delta")) {

results in:

     if ("" + rootAttributes.get("type").equalsIgnoreCase("delta")) {

The line:

    if (("" + rootAttributes.get("type")).equalsIgnoreCase("delta")) {

results in:

     if ("" + rootAttributes.get("type").equalsIgnoreCase("delta")) {
Move to JDT/UI
fixed > I20060331-2000
