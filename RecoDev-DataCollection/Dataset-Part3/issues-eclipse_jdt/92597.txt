In the JDT UI plugin.xml the preferenceTransfer extension does not NLS the name
of the transfer.

In the JDT UI plugin.xml the preferenceTransfer extension does not NLS the name
of the transfer.
fixed > 20050425
