Build 20020212

1. Open a read-only cu (let's say "A.java"
2. Save As...
3. Give a new name (let's say "B.java"

==> "Save Problems" dialog appears which says "B.java" can't be saved because
it's read-only

Very confusing.
Even more confusing: the file is saved anyway

Build 20020212

1. Open a read-only cu (let's say "A.java"
2. Save As...
3. Give a new name (let's say "B.java"

==> "Save Problems" dialog appears which says "B.java" can't be saved because
it's read-only

Very confusing.
Even more confusing: the file is saved anyway
Works in latest.
