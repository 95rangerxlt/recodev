When creating a new template, its default type is JavaDoc.  Perhaps it's just 
my usage, but I always expect the default type to be Java, as I write much more 
Java code than JavaDoc (maybe I shouldn't <g>).

(20020215 stable build)

When creating a new template, its default type is JavaDoc.  Perhaps it's just 
my usage, but I always expect the default type to be Java, as I write much more 
Java code than JavaDoc (maybe I shouldn't <g>).

(20020215 stable build)
fixed > 20020307
