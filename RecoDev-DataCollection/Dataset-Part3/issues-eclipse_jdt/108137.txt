Currently processors asking for the value of the -s option get project-relative
paths. These should be made absolute.

Currently processors asking for the value of the -s option get project-relative
paths. These should be made absolute.
All path-based options now are represented as absolute paths.
