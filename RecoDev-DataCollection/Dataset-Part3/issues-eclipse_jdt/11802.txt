The files are generated without a title.
This results in the browser title starting with ":"

I don't know if this (title) is supported by the standard doclet. If so, we
should allow to set the title in the wizard.

BTW: JDK API doc has a title.

The files are generated without a title.
This results in the browser title starting with ":"

I don't know if this (title) is supported by the standard doclet. If so, we
should allow to set the title in the wizard.

BTW: JDK API doc has a title.
The standard doclet does have a title option. I will add this option to the 
wizard.

JRT.
Title option added. Available in 20020402. JRT.
