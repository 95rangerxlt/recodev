1) Show in>PackageExplorer selects the editor input 
2) "Show in Package Explorer" selects the method.

we should do 2) it reduces tree expansions, and enables the user to quickly 
initiate an operation on the editor input.

1) Show in>PackageExplorer selects the editor input 
2) "Show in Package Explorer" selects the method.

we should do 2) it reduces tree expansions, and enables the user to quickly 
initiate an operation on the editor input.
released a fix to ShowInPackageViewAction - Dirk pls review
