I20050315-1100

When generating a constructor using fields (from Source menu), the resulting
code uses 'this' even if it is not necessary (i.e., fields have an 'f' prefix).
"Qualify all field accesses using 'this'" is disabled.

I20050315-1100

When generating a constructor using fields (from Source menu), the resulting
code uses 'this' even if it is not necessary (i.e., fields have an 'f' prefix).
"Qualify all field accesses using 'this'" is disabled.
Fixed > 20050318
