Currently this action is only available in the editor. It would be convenient 
to also make it available in the outline and package explorer.

The only reason why this currently isn't done are implementation details 
(search is AST based).

Currently this action is only available in the editor. It would be convenient 
to also make it available in the outline and package explorer.

The only reason why this currently isn't done are implementation details 
(search is AST based).
I got tricked by this as well. The helper class JavaElementMapper can be used 
to map a Java element to an AST node.
fixed - thanks Dirk
