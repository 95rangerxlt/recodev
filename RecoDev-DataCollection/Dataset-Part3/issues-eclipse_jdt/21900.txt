Build 2.0

When hovering over the button for selecting a previously opened hierarchy, the 
tooltip reads "Previous Hierarch Inputs", and should rather use the 
word "Focus" given this is the one used in the menu to open/create a hierarchy.

Build 2.0

When hovering over the button for selecting a previously opened hierarchy, the 
tooltip reads "Previous Hierarch Inputs", and should rather use the 
word "Focus" given this is the one used in the menu to open/create a hierarchy.
We should avoid both focus and input. Simply "Previous type hierarchies"
fixed > 20021010
