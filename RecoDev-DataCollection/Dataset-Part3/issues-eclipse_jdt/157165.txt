3.3 M1

I was looking after a test failure and found org.eclipse.jdt.ui.tests.all.AllAllTests just to find out that this was related to the refactoring tests and not JDT UI tests.

Since the tests aren't considered API this should be a trivial change.

3.3 M1

I was looking after a test failure and found org.eclipse.jdt.ui.tests.all.AllAllTests just to find out that this was related to the refactoring tests and not JDT UI tests.

Since the tests aren't considered API this should be a trivial change.
fixed > 20060921
