The declaration and javadoc views are currently only available from the other 
views dialog. We should define short cuts for them in the java perspectives.

The declaration and javadoc views are currently only available from the other 
views dialog. We should define short cuts for them in the java perspectives.
Added them to the "Window > Show view" of Java and Java Browsing perspective.

