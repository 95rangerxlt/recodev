When the cursor is positioned inside a string literal, the Structure viewer
doesn't pop up. This goes for both the Open Structure (Ctrl-F3) and the Show
Outline (Ctrl-O).

When the cursor is positioned inside a string literal, the Structure viewer
doesn't pop up. This goes for both the Open Structure (Ctrl-F3) and the Show
Outline (Ctrl-O).
I'm using the "200211230010" nightly build.
The issue seems to have been fixed with the I200301071430 release.
