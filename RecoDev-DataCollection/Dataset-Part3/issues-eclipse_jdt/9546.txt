i pressume that TestCase creation wizard uses JavaConventions to validate
class names and so on.
error status returned by that class contains messages that can and should be 
shown to the user - we do it everywhere else

i pressume that TestCase creation wizard uses JavaConventions to validate
class names and so on.
error status returned by that class contains messages that can and should be 
shown to the user - we do it everywhere else
fixed
