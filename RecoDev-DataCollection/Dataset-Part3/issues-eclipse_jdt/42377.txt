Preferences > Java > Code Generation > Code and Comments: there's no way to
specify a pattern for a class.

For instance I would like to automatically define a log4j logger (and add the
corresponding import).

Preferences > Java > Code Generation > Code and Comments: there's no way to
specify a pattern for a class.

For instance I would like to automatically define a log4j logger (and add the
corresponding import).
is this bug being processed or has it been lost ?
Currently no action planned for 3.1.
for 3.2 we added type body templates
in 3.2
