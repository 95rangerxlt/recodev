I20050111

The Java editor preference page has nested collapsible sections. With every
thing else beind discussed, get rid of this one.

I20050111

The Java editor preference page has nested collapsible sections. With every
thing else beind discussed, get rid of this one.
fixed for I20050202

replaced the nested sections by groups - not sure whether this is the final
solution, but at least there is no nested collapsible sections.
