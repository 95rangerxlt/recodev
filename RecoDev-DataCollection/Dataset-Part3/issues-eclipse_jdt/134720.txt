Running the code cleanup with all checks (probably it is the removing of this.functionCall) removes method parameters from emthod calls where there has been any change.

Example:

this.setEnabled(false);
-->
setEnabled();


/Daniel

Running the code cleanup with all checks (probably it is the removing of this.functionCall) removes method parameters from emthod calls where there has been any change.

Example:

this.setEnabled(false);
-->
setEnabled();


/Daniel
Moving to UI.
fixed > I20060331-2000

Thanks Daniel! That was ##@% stupit.
