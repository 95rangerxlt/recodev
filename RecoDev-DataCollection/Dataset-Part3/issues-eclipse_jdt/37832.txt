20030506

i always get cought by this:

if (! foo instanceof Bar)

is a syntax error.

it'd be super cool if quick fix could change it to

if (! (foo instanceof Bar))

20030506

i always get cought by this:

if (! foo instanceof Bar)

is a syntax error.

it'd be super cool if quick fix could change it to

if (! (foo instanceof Bar))
added > 20040509
