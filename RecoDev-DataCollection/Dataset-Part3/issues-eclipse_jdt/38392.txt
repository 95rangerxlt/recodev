1) select a  non-java resource in the package explorer
2) refactor>rename
3) a dialog shows up with a preview button
4) press preview
->no preview available

Since the preview is not helpful there should be no preview button.

1) select a  non-java resource in the package explorer
2) refactor>rename
3) a dialog shows up with a preview button
4) press preview
->no preview available

Since the preview is not helpful there should be no preview button.
Adam, 

we should do the same trick as for the "delete refactoring".
Created attachment 5076
patch for RenameRefactoringWizard.java
fixed
