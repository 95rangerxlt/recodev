I20071211-0010

The quick fix which removes unused @SuppressedWarnings is currently:
  'Remove annotation'

This is quite generic. I would expect a name similar to the one used when adding the @SuppressWarnings annoation, e.g.
  'Remove @SuppressWarnings 'serial' from XXX

I20071211-0010

The quick fix which removes unused @SuppressedWarnings is currently:
  'Remove annotation'

This is quite generic. I would expect a name similar to the one used when adding the @SuppressWarnings annoation, e.g.
  'Remove @SuppressWarnings 'serial' from XXX
fixed in M4
