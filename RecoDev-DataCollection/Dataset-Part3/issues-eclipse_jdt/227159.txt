I20080415-0800.

Now that we no longer do a second code assist, we should enable parameter guessing per default.

I20080415-0800.

Now that we no longer do a second code assist, we should enable parameter guessing per default.
We enhanced the current default mode: it inserts in the argument name but also proposes the guessed arguments. The additional option has been reworded.
