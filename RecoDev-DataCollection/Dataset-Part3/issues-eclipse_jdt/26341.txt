Build I20021113

Since ToDo markers are implemented as markers refactorings thinks that the 
method to be inlined has compile errors and therefore doesn't allow inline. 

Must check if more refactorings are effected by this.

Build I20021113

Since ToDo markers are implemented as markers refactorings thinks that the 
method to be inlined has compile errors and therefore doesn't allow inline. 

Must check if more refactorings are effected by this.
Got addressed for M5. To do markers are no longer reported as compile problems.
