Starting up the apt UI plugin produces "preference option missing" errors in 
the error log, in a clean workspace.

Starting up the apt UI plugin produces "preference option missing" errors in 
the error log, in a clean workspace.
Fixed, by setting defaults at the DefaultStore level.
