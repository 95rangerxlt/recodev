If I close my workbench with 'Show Library Projects' enabled, when I restart 
it, this preference is disabled. I have to re-enable it on each startup.

If I close my workbench with 'Show Library Projects' enabled, when I restart 
it, this preference is disabled. I have to re-enable it on each startup.
fixed in build > 207
