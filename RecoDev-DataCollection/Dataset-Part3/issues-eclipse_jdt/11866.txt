"Test Classes to be included" is written with TestClasses (missing space)
The status line talks about methods being selected instead of types.

The action name is written together (TestSuite) instead of Test Suite

"Test Classes to be included" is written with TestClasses (missing space)
The status line talks about methods being selected instead of types.

The action name is written together (TestSuite) instead of Test Suite
And the mnemonic is missing on "Test Classes to be included" (for the check list)
Fixed
