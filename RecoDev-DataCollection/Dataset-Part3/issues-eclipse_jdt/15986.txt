build 0508

Java Browsing perspective.

If you copy a class from somewhere and paste it into a package (very cool, 
btw), it doesn't appear in the types view.
Re-selecting the package to update the type view makes it appear correctly.

build 0508

Java Browsing perspective.

If you copy a class from somewhere and paste it into a package (very cool, 
btw), it doesn't appear in the types view.
Re-selecting the package to update the type view makes it appear correctly.
dani did you fix anything in this area? it works for me in our latest.
Yes I've fixed that one (and some others ;-)
Basically a working copy side effect.

Verified.
