The JUnit preference page is not using the button sizing support from JFace. 
The buttons should look like those on the installed JREs page.

The JUnit preference page is not using the button sizing support from JFace. 
The buttons should look like those on the installed JREs page.
fixed layout
