I would like to run a single test method without changing the 'suite()' method. 
It would be nice if the junit UI would allow to select and run a single method.

(junit.pde)

I would like to run a single test method without changing the 'suite()' method. 
It would be nice if the junit UI would allow to select and run a single method.

(junit.pde)
You can do this in debug mode for failed test cases (Context menu rerun).
fixed
