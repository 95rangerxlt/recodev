Build R2.1

Currently the Java Browsing content provider does a refresh per package fragment
root when a classpath reorder delta comes in instead of a single project refresh.

Build R2.1

Currently the Java Browsing content provider does a refresh per package fragment
root when a classpath reorder delta comes in instead of a single project refresh.
test
This has been fixed a while ago.
