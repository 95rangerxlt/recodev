The setting is persisted as long as I use the standard doclet but gets lost if
after using my custom doclet.

The setting is persisted as long as I use the standard doclet but gets lost if
after using my custom doclet.
Now when the wizard is run after having previously been run with a custom 
doclet the destination settings will appear in the disabled 'destination' 
field. 
Note: if the destination was changed during the previous run (but then a custom 
doclet was used) it will NOT be preserved. Comments?

JRT.
fixed. JRT.
