2.x

Currently not all code in JDT/UI land uses validateEdit to make sure that the 
currently file is editbale. This is especially needed if files are under 
pessimistic version control. Most of the Source actions are affected.

2.x

Currently not all code in JDT/UI land uses validateEdit to make sure that the 
currently file is editbale. This is especially needed if files are under 
pessimistic version control. Most of the Source actions are affected.
*** Bug 20160 has been marked as a duplicate of this bug. ***
*** Bug 20353 has been marked as a duplicate of this bug. ***
fixed >= 20021202
