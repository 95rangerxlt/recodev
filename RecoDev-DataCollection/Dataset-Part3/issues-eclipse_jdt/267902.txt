The current documentation of the batch compiler options doesn't mention the dead code warning.
We need to see if others are missing.

Ideally the documentation should always be in sync with the implementation.

The current documentation of the batch compiler options doesn't mention the dead code warning.
We need to see if others are missing.

Ideally the documentation should always be in sync with the implementation.
Doc seems to be up-to-date now.
Doc was fixed in May.
