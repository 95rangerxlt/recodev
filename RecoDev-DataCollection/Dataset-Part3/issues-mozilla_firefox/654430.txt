Created attachment 529704
CSS match mismatch

When two string CSS property values match they show as unmatched.

e.g.
If the font-family is overridden in a single place in the stylesheet even though it is overridden by the default value it will show as an unmatched rule.

See attached image

Created attachment 529704
CSS match mismatch

When two string CSS property values match they show as unmatched.

e.g.
If the font-family is overridden in a single place in the stylesheet even though it is overridden by the default value it will show as an unmatched rule.

See attached image
