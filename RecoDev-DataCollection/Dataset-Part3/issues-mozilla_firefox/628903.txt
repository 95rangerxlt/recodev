The patch in bug 626745 will seal off evil properties from the Console API. We should remove them after either bug 553102 or bug 625559 land.

The patch in bug 626745 will seal off evil properties from the Console API. We should remove them after either bug 553102 or bug 625559 land.
marking sg:nse (not a security exploit) but sounds like we should keep this bug hidden until after the other fixes are landed.
those bugs have all landed. I'm pretty sure we can mark this fixed. MANY MOONS LATER.
