When using the ctrl/shift modifiers to extend selection, selection should only
be extended when the mouse pointer is in the gutter - i.e. not on the icon or
text portion of the link. Otherwise the link should be loaded using the
modifiers e.g. with ctrl - open link in tab.

When using the ctrl/shift modifiers to extend selection, selection should only
be extended when the mouse pointer is in the gutter - i.e. not on the icon or
text portion of the link. Otherwise the link should be loaded using the
modifiers e.g. with ctrl - open link in tab.
Created attachment 162614
patch
