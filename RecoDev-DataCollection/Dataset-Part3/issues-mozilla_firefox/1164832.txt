As discussed during the Service Workers work week in Madrid, in order to facilitate how to inspect, debug, handle Service Workers, a button to access to the debugger (devtools) for a Service Worker could be added in about:serviceworkers

As discussed during the Service Workers work week in Madrid, in order to facilitate how to inspect, debug, handle Service Workers, a button to access to the debugger (devtools) for a Service Worker could be added in about:serviceworkers
This exists now in about:debugging (not yet functional but it exists :).
