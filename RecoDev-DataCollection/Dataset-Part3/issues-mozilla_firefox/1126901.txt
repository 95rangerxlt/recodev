When a recording is selected, all detail views are populated. Everything would be more snappy if this was done lazily, especially after adding the new memory call tree and flame graph.

When a recording is selected, all detail views are populated. Everything would be more snappy if this was done lazily, especially after adding the new memory call tree and flame graph.
Created attachment 8555992
v1
https://hg.mozilla.org/integration/fx-team/rev/303ec6eb1f8f
https://hg.mozilla.org/mozilla-central/rev/303ec6eb1f8f
