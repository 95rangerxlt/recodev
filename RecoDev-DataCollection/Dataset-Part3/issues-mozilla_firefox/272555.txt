about:config looks empty, but you can select a pref, right-click, e.g. copy name.
So the prefs are there, but invisible.

about:config looks empty, but you can select a pref, right-click, e.g. copy name.
So the prefs are there, but invisible.
Fixed by backing out the branch landing, which restores the trunk-only changes
from bug 221619.
