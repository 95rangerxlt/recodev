


Can we please not repeat this for every release and use the target milestone field instead?
(In reply to comment #1)
> Can we please not repeat this for every release and use the target milestone
> field instead?

Yep, we're working on a better solution than repeating this every six weeks. We'll group all those bugs into topics (like UI, Performance, etc.) and create meta bugs for them. I think we wouldn't even need the target milestone field as version numbers become less important with the train model.
Fx 7 was branched
