>> git commit -m "-"
Should be legal.

A possible example of wanting to use "-" is the screenshot command. That may need a tweak.

>> git commit -m "-"
Should be legal.

A possible example of wanting to use "-" is the screenshot command. That may need a tweak.
New component triage. Filter on "Lobster Thermidor aux crevettes with a Mornay sauce"
