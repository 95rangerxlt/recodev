Since we have some time before the new device sharing flow will actually get implemented, we should validate the design using an interactive mockup.
The flow is quite complex and the purpose of this bugs is to make sure that it feels right. The complexity of the mockup can be determined while building it.

Since we have some time before the new device sharing flow will actually get implemented, we should validate the design using an interactive mockup.
The flow is quite complex and the purpose of this bugs is to make sure that it feels right. The complexity of the mockup can be determined while building it.
Marking this as resolved, because the skeleton is there.  Followup bugs have been filed for future improvements.
