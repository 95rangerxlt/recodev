It should be the "minimum viable app" which doesn't actually do anything. Something you can use as a starting point once you know what you're doing but don't want to recreate each time, and something where you don't have to spend half an hour deleting code before being able to use it.

It should be the "minimum viable app" which doesn't actually do anything. Something you can use as a starting point once you know what you're doing but don't want to recreate each time, and something where you don't have to spend half an hour deleting code before being able to use it.
Here:
https://github.com/mozilla/mortar-privileged-empty-app
