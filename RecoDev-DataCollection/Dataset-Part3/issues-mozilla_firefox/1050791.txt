We want to ship a first version of the timeline. It will be a standalone tool (not part of the performance tab) and it will cover a small fraction of layout operations (restyle, reflow and paint).

We want to ship a first version of the timeline. It will be a standalone tool (not part of the performance tab) and it will cover a small fraction of layout operations (restyle, reflow and paint).
https://developer.mozilla.org/en-US/docs/Tools/Timeline
Moving into the Profiler component. Filter on GUTHRIE'S WAVY CAKES.
We have a v1 now.
