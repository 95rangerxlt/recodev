The function localStorageEnabledTest and the variable isLocalStorageEnabled aren't used anymore, we could remove them.

The function localStorageEnabledTest and the variable isLocalStorageEnabled aren't used anymore, we could remove them.
https://github.com/mozilla/pdf.js/pull/2708
