GCLI commands need a repo in order that they can be easily added to Firefox by GCLI.

A git repo probably makes the most sense.

GCLI commands need a repo in order that they can be easily added to Firefox by GCLI.

A git repo probably makes the most sense.
https://github.com/joewalker/mozcmd
