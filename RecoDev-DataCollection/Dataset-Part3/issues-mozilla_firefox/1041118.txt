Yury: Till said that Shumway's regression tests are running on master, but not the `nat` branch. We need to run the regression tests before we can merge the `nat` branch back to master.

Yury: Till said that Shumway's regression tests are running on master, but not the `nat` branch. We need to run the regression tests before we can merge the `nat` branch back to master.
s/regression/reference/
Fixed a while ago.
