If a browser window is partially off-screen and you are inspecting, the highlighter panel can be drawn outside the browser window boundaries on large elements.

If a browser window is partially off-screen and you are inspecting, the highlighter panel can be drawn outside the browser window boundaries on large elements.
Fixed by bug 642471
