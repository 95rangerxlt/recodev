Once bug 1049825 has ridden the release train and we get some testing on it to reveal any possible hidden bugs, we should enable "invert call tree" by default since it is so much more useful.

Once bug 1049825 has ridden the release train and we get some testing on it to reveal any possible hidden bugs, we should enable "invert call tree" by default since it is so much more useful.
+1, feels right to me.
Fixed in perf++ tool in bug 1102350, will not be fixing in original profiler
