Once the required changes have been made on Google's end, the correct domain to query for suggestions will be "suggestqueries.google.com" and it will contain the parameter "output=firefox".  In the meantime, we should be using "toolbarqueries.google.com" as the query domain.

Once the required changes have been made on Google's end, the correct domain to query for suggestions will be "suggestqueries.google.com" and it will contain the parameter "output=firefox".  In the meantime, we should be using "toolbarqueries.google.com" as the query domain.
Joe, isn't this fixed elsewhere?
Yeah--this was fixed by 340420.  Marking it fixed.
