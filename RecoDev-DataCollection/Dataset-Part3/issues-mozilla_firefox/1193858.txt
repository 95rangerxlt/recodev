Ensure the gh-pages branch points to a built version of the newtab page, for use for development.

The content in the branch will be what gets deployed on a CDN

Ensure the gh-pages branch points to a built version of the newtab page, for use for development.

The content in the branch will be what gets deployed on a CDN
Eventually we will need to move to a server where we can set CSP headers. But this will do for now.
We are using: https://mozilla.github.io/remote-newtab/newTab.html
