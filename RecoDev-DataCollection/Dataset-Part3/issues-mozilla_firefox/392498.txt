That is, any new profiles / new users should get the customizations.

This is a tracking bug for item DIST-00NN in the Firefox 3 PRD.

That is, any new profiles / new users should get the customizations.

This is a tracking bug for item DIST-00NN in the Firefox 3 PRD.
where by "NN", I really mean "1d".
Distribution customizations live in the appdir (See bug 392501)
