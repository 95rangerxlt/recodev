Create a proper LogEntry constructor and prototype. This will handle creating a link and events for handling introspection of logged objects.

The LogEntry introspection UI should be a separate bug

Create a proper LogEntry constructor and prototype. This will handle creating a link and events for handling introspection of logged objects.

The LogEntry introspection UI should be a separate bug
I created a LogMessage constructor and prototype, still iterating:

http://hg.mozilla.org/users/ddahl_mozilla.com/heads-up-display/file/42b38ea8143e/toolkit/components/console/hudservice/HUDUtils.jsm#l107
In patch in bug 534398
