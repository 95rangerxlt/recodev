Right now we're using the feeds icon to represent the Applications prefpane, but presumably we should find/make an icon that represents handling generally, not just feed handling.

Right now we're using the feeds icon to represent the Applications prefpane, but presumably we should find/make an icon that represents handling generally, not just feed handling.
We've since done this (at least, we checked in a temporary generic icon) over in bug 395141.
