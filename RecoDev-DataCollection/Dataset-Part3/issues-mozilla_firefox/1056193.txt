This is what makes the URL loading-related trace tests fail.

STR: run http://localhost:8000/examples/inspector/inspector.html?rfile=../../test/swfs/flash_net_URLRequest.swf with the debugger open.

This is what makes the URL loading-related trace tests fail.

STR: run http://localhost:8000/examples/inspector/inspector.html?rfile=../../test/swfs/flash_net_URLRequest.swf with the debugger open.
Fixed by .. a commit. I can't find it right now, but it wasn't fixed accidentally or anything.
