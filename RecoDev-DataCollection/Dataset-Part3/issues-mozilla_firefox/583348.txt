There are a number of "hacks" in the Sync UI that should be gotten rid of. The ones that should be fixed here are mostly just how the Sync UI interacts with the alltabs menu. Will probably involve a bit of tweaking to the UI and to the alltabs-popup binding in tabbrowser.xml.

There are a number of "hacks" in the Sync UI that should be gotten rid of. The ones that should be fixed here are mostly just how the Sync UI interacts with the alltabs menu. Will probably involve a bit of tweaking to the UI and to the alltabs-popup binding in tabbrowser.xml.
This code is gone.
