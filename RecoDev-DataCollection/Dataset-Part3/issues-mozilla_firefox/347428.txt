When using large system fonts (or a userChrome.css rule), the search bar does not scale to match the current font size while the location bar does.

When using large system fonts (or a userChrome.css rule), the search bar does not scale to match the current font size while the location bar does.
Created attachment 232189
Screenshot
*** Bug 347477 has been marked as a duplicate of this bug. ***
Created attachment 232826
Specify min-height rather than height
Landing on branch.  This bug does not apply to trunk, since the FF2 visual refresh hasn't been applied there.
