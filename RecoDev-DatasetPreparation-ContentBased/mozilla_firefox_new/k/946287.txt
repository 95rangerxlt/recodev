When clicking a widget in the panel menu, it should basically act like a toolbar button and not get a redundant focus ring that's only interesting when using the keyboard.

When clicking a widget in the panel menu, it should basically act like a toolbar button and not get a redundant focus ring that's only interesting when using the keyboard.
Bug 946297 would supposedly fix this.
Fixed by backout of bug 881937.


Widgets in the panel menu get a focus ring when clicked

