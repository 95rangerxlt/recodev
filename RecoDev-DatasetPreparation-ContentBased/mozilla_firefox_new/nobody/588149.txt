The toolbar in the HTML panel is confusing, because it's not specifically for the HTML panel and it's not located where a user would expect it. It should appear at the top of the content area.

The toolbar in the HTML panel is confusing, because it's not specifically for the HTML panel and it's not located where a user would expect it. It should appear at the top of the content area.
this bug is ancient and not relevant for current designs


Inspector: move the toolbar from the HTML panel to the toolbar area of the window

