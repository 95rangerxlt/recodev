This might be fixed by bug 653108, but currently if you close the tab that scratchpad was using for its context, it stops working once you close it.

This might be fixed by bug 653108, but currently if you close the tab that scratchpad was using for its context, it stops working once you close it.
does the Reset menu item in the Context (top-level) menu fix it?
this has been fixed.


Scratchpad breaks if you close the tab is was running in

