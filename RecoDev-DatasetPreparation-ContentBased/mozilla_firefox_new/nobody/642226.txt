It should be possible to use GCLI without a command line in the traditional sense, but using parts of GCLI to provide parameters to a command selected via a keyboard shortcut.

For example the Ace editor could use GCLI to provide input to a search/replace command or a goto-line command without needing a full command line.

It should be possible to use GCLI without a command line in the traditional sense, but using parts of GCLI to provide parameters to a command selected via a keyboard shortcut.

For example the Ace editor could use GCLI to provide input to a search/replace command or a goto-line command without needing a full command line.


GCLI should support use without an input element

