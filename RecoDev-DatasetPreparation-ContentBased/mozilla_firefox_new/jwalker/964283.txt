This is fixed in the GCLI report. Using this bug to track landing.

This is fixed in the GCLI report. Using this bug to track landing.


GCLI 'help' command doesn't return JSONable data (so it won't remote well)

