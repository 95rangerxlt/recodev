And yes, it shouldn't use objects as hashes, but that's a different thing.

And yes, it shouldn't use objects as hashes, but that's a different thing.


GCLI should use 'delete x' only on objects that are used as hashes

