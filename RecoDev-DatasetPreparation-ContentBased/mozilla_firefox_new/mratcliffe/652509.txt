The Style Inspector displays styles that are not yet supported like outline-top-width etc. Because we need to add many CSS properties in case of future support it makes a lot more sense if unsupported styles are not displayed.

The Style Inspector displays styles that are not yet supported like outline-top-width etc. Because we need to add many CSS properties in case of future support it makes a lot more sense if unsupported styles are not displayed.
This is fixed but I will submit the patch as part of bug 582596


Style Inspector displays styles that are not yet supported - they should be skipped

