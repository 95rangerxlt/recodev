After the fix from bug 219405, the first breadcrumb dropdown shows all projects as simple projects. Also Java projects just contain files and folders, rather then Java elements.

After the fix from bug 219405, the first breadcrumb dropdown shows all projects as simple projects. Also Java projects just contain files and folders, rather then Java elements.
We either need to fix this or revert the fix for bug 219405.
Fixed in HEAD.


[breadcrumb] Should not show Java projects as simple projects



Fixed bug 243147: [breadcrumb] Should not show Java projects as simple projects 