I20050510-0010

The UI for adding a library works but I cannot run a class which is inside that
project because the full library path instead of just the directory is set to
the java.library.path variable.

I20050510-0010

The UI for adding a library works but I cannot run a class which is inside that
project because the full library path instead of just the directory is set to
the java.library.path variable.
fixed > 20050511
Verified I20050513-0010


Cannot run after adding a native library

