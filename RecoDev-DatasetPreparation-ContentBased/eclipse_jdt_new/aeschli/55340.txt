When having a ZIP (not JAR) file included as library there can no documentation 
location be selected on its properties page. When I rename the file to "*.jar" 
then it works.

When having a ZIP (not JAR) file included as library there can no documentation 
location be selected on its properties page. When I rename the file to "*.jar" 
then it works.
fixed > 20040319

fixed if the element is a IPackageFragmentRoot (in Java views), but for all
IFiles with file suffix 'zip' (would be to much)


Documentation (Javadoc) location selector does not show up with ZIP files



55340 Documentation (Javadoc) location selector does not show up with ZIP files 