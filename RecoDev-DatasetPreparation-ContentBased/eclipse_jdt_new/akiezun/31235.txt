I20030206

- Java project with two folder src1 and src2 where src1 is on the build path
  (e.g. source folder)
- select src2 and start DnD and move it over src2.

Observe: DnD is enabled.

I20030206

- Java project with two folder src1 and src2 where src1 is on the build path
  (e.g. source folder)
- select src2 and start DnD and move it over src2.

Observe: DnD is enabled.
no exception or anything but should be easy to fix
let's say P3, RC1
fixed


Move: source accepts itself as target [DnD] [ccp]



fix for 31235 Move: source accepts itself as target [DnD] [ccp] 