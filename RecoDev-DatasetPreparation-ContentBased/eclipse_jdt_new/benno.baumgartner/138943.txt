N20060427

This does not scale at all and can be done much faster.

N20060427

This does not scale at all and can be done much faster.
fixed > 20060426

Creating one AST per cu plus using the batch mode of the compiler. This should be redicules faster. 


[clean up] CorrectionMarkerResolution.findOtherMarkers(IMarker[]) creates AST for each marker



138943 [clean up] CorrectionMarkerResolution.findOtherMarkers(IMarker[]) creates AST for each marker 