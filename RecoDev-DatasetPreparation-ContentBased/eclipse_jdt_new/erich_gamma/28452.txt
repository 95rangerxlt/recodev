Build 20021216

The Java->JUnit preference page uses "Stack filter patterns".
"Stack trace filters" or "Stack filters" would be better. 

Note: "Stack filters" is used in several places (hovers, the class selection
dialog, package selection dialog...).

Build 20021216

The Java->JUnit preference page uses "Stack filter patterns".
"Stack trace filters" or "Stack filters" would be better. 

Note: "Stack filters" is used in several places (hovers, the class selection
dialog, package selection dialog...).
fixed


[JUnit] Preference page: stack filter should be stack trace filter



28452   [JUnit] Preference page: stack filter should be stack trace filter 