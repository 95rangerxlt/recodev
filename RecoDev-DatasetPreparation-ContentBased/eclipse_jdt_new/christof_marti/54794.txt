Project settings are not honored. We should use the new chained preference store
instead of the JDT/CORE plugin store.

Project settings are not honored. We should use the new chained preference store
instead of the JDT/CORE plugin store.
Available in builds >= I20040316


[syntax highlighting] assert keyword not highlighted according to project settings

