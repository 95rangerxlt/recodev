Right now, you'll have to re-enable it every time you restart the toolbox, and for each tab. It would be easier if it was just hooked up to a pref.

Would need a pref watcher as well to synchronize the checkbox and call tree view with the pref.

Right now, you'll have to re-enable it every time you restart the toolbox, and for each tab. It would be easier if it was just hooked up to a pref.

Would need a pref watcher as well to synchronize the checkbox and call tree view with the pref.
Added in perf++ tool in bug 1102350, will not be fixing in original profiler


"invert call tree" should be backed by a pref so it is persistent

