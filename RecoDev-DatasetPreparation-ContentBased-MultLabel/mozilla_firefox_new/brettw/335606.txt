The current spellchecking dictionary doesn't get automatically selected as the default.

The current spellchecking dictionary doesn't get automatically selected as the default.
This was (branch) / will be (trunk) fixed by the patch to bug 335607.


Make the dictionary work by default

fixed1.8.1