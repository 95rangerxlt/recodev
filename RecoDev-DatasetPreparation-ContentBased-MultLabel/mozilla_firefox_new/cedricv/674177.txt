Scratchpad can use Orion for editing (preffed off by default for now), Style Editor should do the same.

Scratchpad can use Orion for editing (preffed off by default for now), Style Editor should do the same.
Style Editor now uses SourceEditor/Orion, it will land with it directly (more code reuse, let's duplication of effort to handle textbox stuff).
s/let's/less


Style Editor should use Orion for editing

