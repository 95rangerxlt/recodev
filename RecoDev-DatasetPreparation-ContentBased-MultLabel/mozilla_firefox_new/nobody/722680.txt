After closing the second-to-last tab, the close button disappears from the last (now only) tab only after the closing animation has ended. The UI would feel better if the close button was removed immediately.

After closing the second-to-last tab, the close button disappears from the last (now only) tab only after the closing animation has ended. The UI would feel better if the close button was removed immediately.


Remove the tab close button immediately when the second-to-last tab is closed

