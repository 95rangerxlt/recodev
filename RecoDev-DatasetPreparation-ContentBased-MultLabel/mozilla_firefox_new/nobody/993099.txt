I can open the split console in the add-on debugger window but it doesn't work at all. We should either disable the binding fort that window or ( ideally ) get it to work, and turn on the console tool pane as well.

I can open the split console in the add-on debugger window but it doesn't work at all. We should either disable the binding fort that window or ( ideally ) get it to work, and turn on the console tool pane as well.
Bug 993029 has made the split console work


Split console does not work in the add-on debugger, but is still available.

