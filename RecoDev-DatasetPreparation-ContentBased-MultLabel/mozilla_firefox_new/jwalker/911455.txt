Way back when GCLI was embedded in the web console we had a shift-enter keyboard shortcut to take the current input and open it in Scratchpad. We've not used this code in ages, and the planned integration with JSTerm doesn't use it and trips over it, so remove it.

Way back when GCLI was embedded in the web console we had a shift-enter keyboard shortcut to take the current input and open it in Scratchpad. We've not used this code in ages, and the planned integration with JSTerm doesn't use it and trips over it, so remove it.


Remove the GCLI->Scratchpad link code from GCLI

