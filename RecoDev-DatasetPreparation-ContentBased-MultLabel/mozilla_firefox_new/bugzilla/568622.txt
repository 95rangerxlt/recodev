Need to figure out why we don't see any http logging when you invoke a second console display.

Need to figure out why we don't see any http logging when you invoke a second console display.
This appears to be fixed in my latest build. I have been hacking away inside the httpActivityObserver.

Will re-open if this regresses.


http logging fails on second and subsequent HeadsUpDisplays

