The script editor appears empty when the debugger is first loaded, even when the script list is not empty. Only the change event in the script list loads the editor with text.

The script editor appears empty when the debugger is first loaded, even when the script list is not empty. Only the change event in the script list loads the editor with text.
Fixed in https://hg.mozilla.org/users/dcamp_campd.org/remote-debug/rev/cfdba068852c


The script editor appears empty even with scripts in the page

