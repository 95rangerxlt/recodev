M5

Java editors are unreadable in RTL so we should make them always LTR.

M5

Java editors are unreadable in RTL so we should make them always LTR.
Fixed in HEAD.
Available in builds > 20050224.


Java editor should override getOrientation()



Fixed bug 86326: Java editor should override getOrientation() 