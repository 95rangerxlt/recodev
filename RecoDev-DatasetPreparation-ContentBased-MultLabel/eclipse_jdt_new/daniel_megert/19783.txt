When I export Java source code only into a JAR, the GUI should not ask me
questions like "export class files with compile errors?" or "Build project if
not build automatically?".

When I export Java source code only into a JAR, the GUI should not ask me
questions like "export class files with compile errors?" or "Build project if
not build automatically?".
I assume you mean the options should be disabled in the wizard.

Yes, that'd be fine.
Fixed.
Available in 2.1 builds > 20020722

Since milestones are now available, changing milestone from "2.1" to "2.1 M1".


When I export source code into a JAR, GUI asks non-applying questions.



Bug 197834 Expression Evaluation Failure with Generics 

Fixed bugs 15877, 20153, 19783 