The implementation of the existig IWorkingCopyManager should forward to the new 
support for managed working copies provided by Java core.

If possible the IWorkingCopyManager should be deprecated.

The implementation of the existig IWorkingCopyManager should forward to the new 
support for managed working copies provided by Java core.

If possible the IWorkingCopyManager should be deprecated.
Build 20020508.


Leverage new Java Core support for managed working copies

