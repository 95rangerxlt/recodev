Build 204
While trying to type the following in a random class:
new File(super.getPath());

the automatic code assist inserts a bogus value when "." is typed:

new File(superClass.

Build 204
While trying to type the following in a random class:
new File(super.getPath());

the automatic code assist inserts a bogus value when "." is typed:

new File(superClass.
Build 20011218.


Automatic Code Assist needs to be smarter #3



Fix for bug 394967 - PackageFragment.getAttachedJavadoc returns the complete HTML page rather than the package documentation section. 

Fix for bug 394967 - PackageFragment.getAttachedJavadoc returns the complete HTML page rather than the package documentation section. 

4964, 4966, 4967, 4968, 4969, 4999 