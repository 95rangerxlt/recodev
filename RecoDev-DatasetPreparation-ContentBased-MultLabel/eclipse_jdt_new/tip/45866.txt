I20031029

- create a test case with an interface I and the three class A, B, C where B 
  extends A and C extends B. All classes implement interface I.

- test method
		B b= new C();
		b.toString();

- select b and activate Change Type.

observe: the proposal misses the interface I

I20031029

- create a test case with an interface I and the three class A, B, C where B 
  extends A and C extends B. All classes implement interface I.

- test method
		B b= new C();
		b.toString();

- select b and activate Change Type.

observe: the proposal misses the interface I
Frank, please close if this is fixed in the latest.
This is fixed in the latest version.


Change Type missing interface [refactoring]

