We should use colored labels in the proposal popup, e.g. extra information in gray.

Prefix matching would need JDT Core support (e.g. for camel case stuff).

We should use colored labels in the proposal popup, e.g. extra information in gray.

Prefix matching would need JDT Core support (e.g. for camel case stuff).


*** This bug has been marked as a duplicate of bug 202470 ***
Doesn't sound like a dup of bug 202470...
Yes, indeed. Got the wrong bug. Thanks.
This is fixed.


[content assist] use colored labels in proposal popup

