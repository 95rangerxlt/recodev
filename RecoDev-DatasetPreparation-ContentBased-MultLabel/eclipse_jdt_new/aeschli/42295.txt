It would be nice to have Quick assist to implement an interface on a class you 
just used as the interface.

E.g.

XYZ xyz;
JButton b = new JButton("test");
b.setAction(xyz); // suggest to add implements Action to the XYZ class.

It would be nice to have Quick assist to implement an interface on a class you 
just used as the interface.

E.g.

XYZ xyz;
JButton b = new JButton("test");
b.setAction(xyz); // suggest to add implements Action to the XYZ class.
added > 20040429


Make p1 implement interface X when using p1 as an X [quick fix]

