20060228

As sson as platform ui added the new constant we will add it to the default views (in the view menu only)

layout.addShowViewShortcut(IPageLayout.ID_PROGRESS_VIEW);

20060228

As sson as platform ui added the new constant we will add it to the default views (in the view menu only)

layout.addShowViewShortcut(IPageLayout.ID_PROGRESS_VIEW);
platform bug is bug 129184
fixed > 20060307


[java perspective] add progress view to view shortcuts



129730 [java perspective] add progress view to view shortcuts 