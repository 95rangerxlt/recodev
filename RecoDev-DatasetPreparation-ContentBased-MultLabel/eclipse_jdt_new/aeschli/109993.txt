I20050920-0010

If there are more $NON-NLS$ tags (and corresponding warnings) it would be nice
to remove all unused tags instead of letting the user do one by one.

I20050920-0010

If there are more $NON-NLS$ tags (and corresponding warnings) it would be nice
to remove all unused tags instead of letting the user do one by one.
Start the clean up action or hit Ctrl-Enter on the "Remove unused non nls tag" proposal to remove them all.


[quick fix] Remove $NON-NLS$ tag quick fix could remove all

