RC1:
- given a class C with these methods:
     int C() {}
     int foo() {}
   using QF for C() results in a "Change to constructor"
   and for foo() results in "change to C(...)"

RC1:
- given a class C with these methods:
     int C() {}
     int foo() {}
   using QF for C() results in a "Change to constructor"
   and for foo() results in "change to C(...)"
We should use consistent wording here.
fixd > 20030303


inconsistent wording in "Change to Constructor" QF



Fixed bug 329248: [ltk] "A file or folder with this name already exist" on empty input for resource rename dialog 

32924 inconsistent wording in "Change to Constructor" QF 