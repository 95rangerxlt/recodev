class Example {
  public Example(Object object) {
    this.object = object;
  }
}

Quick Fix doesn't provide a solution for "this.object" - "create field" would 
be appropriate.

class Example {
  public Example(Object object) {
    this.object = object;
  }
}

Quick Fix doesn't provide a solution for "this.object" - "create field" would 
be appropriate.
fixed > 20020531


Quick Fix: no "create field" when prefixed with "this."



16230 Quick Fix: no "create field" when prefixed with "this." 