I20040219

If a launch configuration specifies multiple arguments they appear as single <string> tag in the 
Info.plist file.
They should be converted into an <array> tag.

I20040219

If a launch configuration specifies multiple arguments they appear as single <string> tag in the 
Info.plist file.
They should be converted into an <array> tag.
fixed for I20040224


AppBundleExporter: generates incorrect tag in Info.plist for arguments

