I20040427

Syntactic highlighting preference page: Use "constant" instead of "static final".

Reasons:
1) we use contant elsewhere
2) it also covers final static

I20040427

Syntactic highlighting preference page: Use "constant" instead of "static final".

Reasons:
1) we use contant elsewhere
2) it also covers final static
has been fixed while moving the semantic HL to syntax HL page



[wording][preferences] Use "constant" instead of "static final"

