When there is a test failure we should support to re-run the failing test 
under the debugger

When there is a test failure we should support to re-run the failing test 
under the debugger
the context menu in the hierarchy and failure view now provides both a run and 
debug option 


[JUnit] Add support to re-run a test under the debugger

