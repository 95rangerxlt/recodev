Introduce factory uses internal core API (JavaModelStatus). 

Instead of throwing a JavaModelException the code could simply throw a
CoreException where all building blocks are API.

Introduce factory uses internal core API (JavaModelStatus). 

Instead of throwing a JavaModelException the code could simply throw a
CoreException where all building blocks are API.
Got fixed for M5.


Introduce Factory uses internal Core API

